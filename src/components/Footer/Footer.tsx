import { Bounded } from '@/ui/Bounded';

export default function Footer() {
  return (
    <footer>
      <Bounded yPadding='lg' className='relative'>
        footer
      </Bounded>
    </footer>
  );
}
